This project was made to display products on a dashboard that shows each products photo, price, name, and availability. It also has the functionality to remove a product be clicking the remove button.

To set this project up, run "npm run dev" in the terminal and click on the link.

Run npm test to run the tests.